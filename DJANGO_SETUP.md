# Инструкция по настройке Django проекта AutoTrade

## Установка зависимостей

```bash
pip install -r requirements.txt
```

## Первоначальная настройка

1. **Создайте миграции:**
```bash
python manage.py makemigrations
python manage.py migrate
```

2. **Создайте суперпользователя для доступа к админке:**
```bash
python manage.py createsuperuser
```
Введите имя пользователя, email и пароль.

3. **Создайте начальные данные (страны и типы кузова):**
```bash
python manage.py shell
```

Затем выполните:
```python
from catalog.models import Country, CarType

# Создаем страны
Country.objects.get_or_create(
    code='japan',
    defaults={
        'name': 'Япония',
        'description': 'Автомобили из Японии - это качество, надежность и передовые технологии.',
        'flag_emoji': '🇯🇵'
    }
)

Country.objects.get_or_create(
    code='korea',
    defaults={
        'name': 'Корея',
        'description': 'Корейские автомобили сочетают в себе современный дизайн, отличное качество и доступные цены.',
        'flag_emoji': '🇰🇷'
    }
)

Country.objects.get_or_create(
    code='china',
    defaults={
        'name': 'Китай',
        'description': 'Китайские автомобили - это отличное соотношение цены и качества.',
        'flag_emoji': '🇨🇳'
    }
)

# Создаем типы кузова
types = ['Седан', 'Хэчбек', 'Кроссовер', 'Внедорожник', 'Универсал', 'Купе', 'Кабриолет', 'Минивэн']
for type_name in types:
    CarType.objects.get_or_create(
        slug=type_name.lower().replace('ё', 'е'),
        defaults={'name': type_name}
    )
```

## Запуск сервера

```bash
python manage.py runserver
```

Откройте в браузере:
- Сайт: http://127.0.0.1:8000
- Админка: http://127.0.0.1:8000/admin

## Использование админки

1. Войдите в админку по адресу http://127.0.0.1:8000/admin
2. Используйте созданного суперпользователя
3. В разделе "Каталог автомобилей" вы можете:
   - Добавлять/редактировать автомобили
   - Загружать фотографии
   - Указывать страну и тип кузова
   - Добавлять описания
   - Управлять дополнительными изображениями

## Структура моделей

- **Country** - Страны (Япония, Корея, Китай)
- **CarType** - Типы кузова (Седан, Хэчбек, Кроссовер и т.д.)
- **Car** - Автомобили с полями:
  - Марка, модель, год
  - Страна (ForeignKey)
  - Тип кузова (ForeignKey)
  - Цена, описание
  - Главное изображение
  - Статус активности
- **CarImage** - Дополнительные изображения автомобиля

## Папки для медиа файлов

- Загруженные изображения сохраняются в `media/cars/`
- Галерея изображений в `media/cars/gallery/`

